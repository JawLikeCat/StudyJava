JVM主要的三块内存空间，栈内存、堆内存、方法区内存。
    1、栈内存
        在方法被调用的时候，该方法需要的内存空间在栈内存中分配；
        先进后出，后进先出；
        栈帧永远指的的是栈顶部的元素；
        处于栈顶部的元素具有活跃权。
    2、堆内存
    3、方法区内存
        类加载器classloader将硬盘上的xxx.class字节装载到JVM的时候，会将字节码文件存放到方法区当中，也就是说方法区中存储的是代码片段；
        因为类需要加载，所以方法区中最先有数据；